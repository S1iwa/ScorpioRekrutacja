#include <memory>
#include "tester.hpp"

int solution_main(std::shared_ptr<backend_interface::Tester> tester) {
  std::ignore = tester;  // Prevent unused variable warning - you can remove this
  return 0;
}
